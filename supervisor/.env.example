# Supervisor Environment Configuration
# Copy this file to .env and adjust values as needed

# Server Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=arx_supervisor
DB_SSLMODE=disable

# Goose Migration Configuration
GOOSE_DRIVER=postgres
GOOSE_DBNAME=arx_supervisor
GOOSE_USER=postgres
GOOSE_PASSWORD=password
GOOSE_HOST=localhost
GOOSE_PORT=5432
GOOSE_SSLMODE=disable

# Routing Configuration
K_NEAREST=3
MAX_DISTANCE=50.0
LOAD_WEIGHT=0.6
DISTANCE_WEIGHT=0.4

# Health Monitoring Configuration
HEALTH_CHECK_INTERVAL=30
HEALTH_TIMEOUT=5
HEALTH_FAILURE_THRESHOLD=3